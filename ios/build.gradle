sourceSets.main.java.srcDirs = [ "src/" ]

sourceCompatibility = '1.7'
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

ext {
	mainClassName = "fandy.dev.debtcollector.IOSLauncher"
}

launchIPhoneSimulator.dependsOn build
launchIPadSimulator.dependsOn build
launchIOSDevice.dependsOn build
createIPA.dependsOn build

robovm {
    arch = "arm64"
    iosSignIdentity = "F1F09A790480C9CD2272BB787B881AF6D78097AB"
        //To find iosSignIdentity, run this in terminal: security find-identity -v -p codesigning

    iosProvisioningProfile = "fajarmuhf@gmail.com"
        //The simple name of the profile. Look in Xcode preferences->accounts.

    iosSkipSigning = false
    stdoutFifo = null
    stderrFifo = null
}

eclipse.project {
    name = appName + "-ios"
    natures 'org.robovm.eclipse.RoboVMNature'
}